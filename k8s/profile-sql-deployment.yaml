apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-sql-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vybes-sql-profiles
  template:
    metadata:
      labels:
        app: vybes-sql-profiles
    spec:
      containers:
        - name: vybes-sql-profiles
          image: mcr.microsoft.com/mssql/server:2019-latest
          ports:
            - containerPort: 1433
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: SA_PASSWORD
              value: "Pimpas123"
          volumeMounts:
            - mountPath: /var/opt/mssql/data
              name: mssqldb
      volumes:
        - name: mssqldb
          persistentVolumeClaim:
            claimName: mssql-claim
---
apiVersion: v1
kind: Service
metadata:
  name: profile-sql-lb
spec:
  type: LoadBalancer
  selector:
    app: vybes-sql-profiles
  ports:
    - protocol: TCP
      port: 7001
      targetPort: 1433
